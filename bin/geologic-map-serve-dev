#!/usr/bin/env bash

cd $(git rev-parse --show-toplevel)

dist=./extensions/web/dist

parcel ./extensions/web/index.html \
  --out-dir $dist &
pid1=$!

nodemon --ignore $dist ./src/geologic-map serve &
pid2=$!

wait $pid1 $pid2
