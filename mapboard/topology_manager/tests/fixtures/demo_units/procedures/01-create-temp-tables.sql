CREATE TABLE tmp_linework_type (
    id text PRIMARY KEY,
    name text,
    color text,
    layer text
);

CREATE TABLE tmp_polygon_type (
    id text PRIMARY KEY,
    name text,
    color text,
    symbol text,
    symbol_color text,
    layer text
);
