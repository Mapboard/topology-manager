# Claude Code guidance for topology-manager

All agent guidance for this repository lives in [`AGENTS.md`](AGENTS.md) — read it
first and follow it. It covers what the library does, its configuration axes, the
update pipeline, how to run and validate against the test database, and the
conventions and pitfalls that matter when changing SQL fixtures or the face loop.
